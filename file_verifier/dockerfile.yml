FROM python:3.11-slim

RUN apt-get update && apt-get install -y clamav && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY verify_files.py .

CMD ["sh", "-c", "while true; do python verify_files.py; sleep 3600; done"]